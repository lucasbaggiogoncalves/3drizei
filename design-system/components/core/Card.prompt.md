Soft-rounded white surface with warm shadow. The base container for content blocks.

```jsx
<Card interactive padding="lg">…</Card>
```

Props: `interactive` (hover lift), `padding` (`none | sm | md | lg`).
