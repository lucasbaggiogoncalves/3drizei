Filter or category chip — selectable, optionally removable. Use in catalog filter rows.

```jsx
<Tag selected>Presentes</Tag>
<Tag removable onRemove={() => {}}>Aniversário</Tag>
```

Props: `selected`, `removable` + `onRemove`.
