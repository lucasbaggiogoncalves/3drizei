Circular icon-only button. Pass a Lucide icon node as children; always provide `label` for a11y.

```jsx
<IconButton label="Favoritar" variant="soft"><i data-lucide="heart" /></IconButton>
```

Variants: `soft` (terracotta tint), `solid`, `outline`, `ghost`. Sizes `sm | md | lg` (44px default touch target).
