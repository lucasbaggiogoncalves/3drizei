Labeled text input with optional leading icon, hint, and error state.

```jsx
<Input label="Seu nome" placeholder="Como devo gravar?" leftIcon={<i data-lucide="user" />} />
<Input label="E-mail" error="E-mail inválido" />
```

Focus shows the terracotta ring. Pass `error` to flag the field red.
